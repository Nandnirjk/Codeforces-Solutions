#include <bits/stdc++.h>
using namespace std;
 
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
 
    long long N, M;
    cin >> N >> M;
 
    if (M == 1) {
        cout << N - 1 << '\n';
    } else {
        cout << N * (M - 1) << '\n';
    }
 
    return 0;
}
